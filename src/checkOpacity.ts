import * as fs from 'fs';
import { parse } from 'url';

(async () => {
    // using a simple base64 output of a tiny portion or just import pngjs?
    // wait, pngjs might not be installed. Let's check package.json
})()
